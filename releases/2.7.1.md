# 2.7.1

Adds configurable `maxContextWindowTokens` and `maxOutputTokens` number settings (following the Producer pattern), so Chief of Staff model planning ceilings and per-response budgets — including reasoning — can be tuned to the selected model. Output tokens are clamped to 2,048–32,768 and to less than the configured context window.
